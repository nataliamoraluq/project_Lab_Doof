<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="https://i.redd.it/grmu4umzy7c51.png">
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}" type="text/css"/>
    <title> Doofenshmirtz "Evil" Lab Inc. </title>
</head>
<body>
    <head>
        <nav id="navBarOptions">
            <a id="navIndic" href="{{ url_for('indications') }}"> Indications</a>
            <a href="{{url_for('categories')}}"> Categories</a> 
            <a href="{{url_for('exams')}}"> Exams & Services</a>
            <a href="{{url_for('showCatalogue')}}"> See Catalogue</a>
            <a href="{{url_for('showReport')}}"> See Report</a>
            <a href="{{url_for('showVideo')}}"> Doof. Inc.</a>
        </nav>
    </head>
    {% for message in get_flashed_messages()  %}
        <p style="color: red; font-weight: 600">{{ message }}</p>
    {% endfor %}
    <div class="contentMainBlock">
        {% block contentBase %} {% endblock %}
    </div>
</body>
</html>